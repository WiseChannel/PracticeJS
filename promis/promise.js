import { reject } from "q";
import {async } fromBits
import { fromBits } from "long";

    const promise = new Promise(resolve,reject)

    promise (resolve,reject) => {
        .then(resolve) {
            console.log('fine');
        }
        .then(reject) {
            console.error('Error');
        }
    }